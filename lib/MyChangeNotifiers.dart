import 'package:flutter/material.dart';
class MyChangeNotifiers extends StatelessWidget {
  MyChangeNotifiers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body:Container() ,
    );
  }
}
